dependencies:
  idf: '>=5.5.4'

  cpp_bus_driver:
    override_path: "../libraries/cpp_bus_driver"

  private_library:
    override_path: "../libraries/private_library"
    matches:
      - if: "$CONFIG{EXAMPLE_BUILD_SCREEN} == True"

  st7701_driver:
    override_path: "../libraries/st7701_driver"
    matches:
      - if: "$CONFIG{EXAMPLE_BUILD_SCREEN} == True"
